<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetBuddy1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Brand Colors -->
        <item name="colorPrimary">@color/brand_green</item>
        <item name="colorSecondary">@color/brand_purple</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/brand_green</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Global Text Colors -->
        <item name="android:textColorPrimary">@color/primary_text</item>
    </style>

    <style name="Theme.BudgetBuddy1" parent="Base.Theme.BudgetBuddy1" />
</resources>